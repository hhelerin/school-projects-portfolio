@model App.DTO.v1.SchoolConfiguration.UpdateStudioInputModel
@{
    ViewData["Title"] = "Edit Studio";
    var slug = ViewData["Slug"] as string;
    var studioId = ViewData["StudioId"] as Guid?;
}

<div class="container mt-4">
    <h1>Edit Studio</h1>

    <div class="row">
        <div class="col-md-6">
            <form asp-action="Edit" asp-route-slug="@slug" asp-route-id="@studioId" method="post">
                @Html.AntiForgeryToken()
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                <div class="mb-3">
                    <label asp-for="Name" class="form-label"></label>
                    <input asp-for="Name" class="form-control" />
                    <span asp-validation-for="Name" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="ContactInfo" class="form-label">Contact Information</label>
                    <textarea asp-for="ContactInfo" class="form-control" rows="2"></textarea>
                    <span asp-validation-for="ContactInfo" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="Details" class="form-label"></label>
                    <textarea asp-for="Details" class="form-control" rows="3"></textarea>
                    <span asp-validation-for="Details" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <button type="submit" class="btn btn-primary">Save Changes</button>
                    <a asp-action="Index" asp-route-slug="@slug" class="btn btn-secondary">Cancel</a>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}